# 剧本管理移动端 v1.6

本版重点：

- 项目与剧集抽屉改为紧凑连续目录，减少无效空白。
- 展开符号和版本圆点使用固定网格居中。
- 思维导图五层使用黄、橙、绿、蓝、紫柔和纯色区分。
- 手机主屏幕 App 底栏真正贴底，安全区只计算一次。
- 保留原文默认显示、连续搜索不掉键盘、单指拖动与双指缩放等功能。

剧本 JSON 仍只保存在当前设备浏览器的 IndexedDB 中，不会上传 GitHub。
